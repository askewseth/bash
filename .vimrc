color torte 
syntax on

set number "Line numbering

set ruler "Set the ruler on the bottom of the screen. ex) x,y 

set background=light

set ignorecase "ignore case on search

set autoindent

set confirm "confirms if you want to save on exit

"python compliance
set tabstop=4
set softtabstop=4

set cursorline "sets the underline on the current line

set wildmenu "visual autocomplete for command menu

set showmatch "highlight matching for braces
